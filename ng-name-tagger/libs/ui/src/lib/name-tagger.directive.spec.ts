import { NameTaggerDirective } from './name-tagger.directive';

describe('NameTaggerDirective', () => {
  it('should create an instance', () => {
    const directive = new NameTaggerDirective();
    expect(directive).toBeTruthy();
  });
});
